{
  "name": "cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/cli/src",
  "targets": {
    "build": {
      "executor": "@shell-shock/nx:build",
      "dependsOn": ["^build"],
      "defaultConfiguration": "production",
      "options": {
        "configFile": "apps/cli/shell-shock.config.ts",
        "outputPath": "apps/cli/dist",
        "copyPath": "dist/apps/cli"
      },
      "configurations": {
        "production": {
          "mode": "production"
        },
        "development": {
          "mode": "development",
          "skipCache": true
        }
      }
    },
    "nx-release-publish": {
      "executor": "@storm-software/workspace-tools:npm-publish"
    }
  }
}
