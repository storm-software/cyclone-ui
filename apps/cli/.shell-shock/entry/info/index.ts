/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import { showBanner } from "shell-shock:banner/info";
import { textColors, writeLine } from "shell-shock:console";
import { env, isDebug, isDevelopment } from "shell-shock:env";
import { showHelp } from "shell-shock:help/info";
import {
  type GlobalOptions,
  isHelp,
  useArgs,
  useGlobalOptions,
  withCommand
} from "shell-shock:state";
import { executeUpdate } from "shell-shock:update";
import { isInteractive } from "shell-shock:utils";
import handleInfo from "../../../src/commands/info/command";

// Generated by Power Plant
// Please do not edit this file manually - it will be automatically overwritten
// the next time the generator is run.

export interface InfoOptions extends GlobalOptions {
  /**
   * The working directory.
   */
  cwd?: string | undefined;
  /**
   * Output machine-readable JSON.
   */
  json?: boolean | undefined;
}

/**
 * The Project Information (cyclone-ui info) command.
 *
 * @remarks
 * Show Cyclone UI project configuration and installed components.
 *
 * @title Project Information
 *
 * @param args - The command-line arguments passed to the command.
 */
export async function handler(args: string[] = useArgs()) {
  const options = {
    ...useGlobalOptions()
  } as unknown as InfoOptions;

  for (let i = 0; i < args.slice(0).length; i++) {
    const arg: string =
      args[i].length > 3 && args[i].startsWith("--")
        ? `--${args[i].slice(2).replaceAll("-", "").toLowerCase().replaceAll("-", "").replaceAll("_", "")}`
        : args[i].length > 2 && args[i].startsWith("-")
          ? `-${args[i].slice(1).replaceAll("-", "")}`
          : args[i];
    if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd"
    ) {
      if (
        /^--?(cwd)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.cwd = arg
          .replace(/^--?(cwd)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.cwd = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("json=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "json" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("json=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "json"
    ) {
      if (
        /^--?(json)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(json)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.json =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.json = true;
      }
    }
  }

  await showBanner();
  await executeUpdate();

  if (!isHelp()) {
    writeLine("");
    if (isDevelopment || isDebug || env.LOG_LEVEL === "debug") {
      writeLine(
        textColors.body.tertiary(
          "Debug mode is enabled. Additional debug information may be logged to the console."
        )
      );
    }
  }

  if (!isInteractive) {
    const failures: string[] = [];
  }

  if (options.help) {
    return showHelp();
  } else {
    return withCommand("info", ["info"], [options], handleInfo);
  }
}
