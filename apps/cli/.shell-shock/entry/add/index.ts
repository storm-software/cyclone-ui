/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import { showBanner } from "shell-shock:banner/add";
import { textColors, writeLine } from "shell-shock:console";
import { env, isDebug, isDevelopment } from "shell-shock:env";
import { showHelp } from "shell-shock:help/add";
import {
  type GlobalOptions,
  isHelp,
  useArgs,
  useGlobalOptions,
  withCommand
} from "shell-shock:state";
import { executeUpdate } from "shell-shock:update";
import { isInteractive } from "shell-shock:utils";
import handleAdd from "../../../src/commands/add/command";

// Generated by Power Plant
// Please do not edit this file manually - it will be automatically overwritten
// the next time the generator is run.

export interface AddOptions extends GlobalOptions {
  /**
   * The working directory.
   */
  cwd?: string | undefined;
  /**
   * Overwrite existing files.
   */
  overwrite?: boolean | undefined;
  /**
   * Add every available component.
   */
  all?: boolean | undefined;
  /**
   * Override the configured component output path.
   */
  path?: string | undefined;
  /**
   * Preview changes without writing files.
   */
  dryRun?: boolean | undefined;
  /**
   * Show the changes as a diff without writing files.
   */
  diff?: boolean | undefined;
  /**
   * Show resolved file contents without writing files.
   */
  view?: boolean | undefined;
  /**
   * Mute non-essential output.
   */
  silent?: boolean | undefined;
  /**
   * Override the configured registry API URL.
   */
  registry?: string | undefined;
}

/**
 * The Add (cyclone-ui add) command.
 *
 * @remarks
 * Adds a new component to the project.
 *
 * @title Add
 *
 * @param args - The command-line arguments passed to the command.
 */
export async function handler(args: string[] = useArgs()) {
  const options = {
    ...useGlobalOptions()
  } as unknown as AddOptions;

  for (let i = 0; i < args.slice(0).length; i++) {
    const arg: string =
      args[i].length > 3 && args[i].startsWith("--")
        ? `--${args[i].slice(2).replaceAll("-", "").toLowerCase().replaceAll("-", "").replaceAll("_", "")}`
        : args[i].length > 2 && args[i].startsWith("-")
          ? `-${args[i].slice(1).replaceAll("-", "")}`
          : args[i];
    if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd"
    ) {
      if (
        /^--?(cwd)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.cwd = arg
          .replace(/^--?(cwd)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.cwd = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("overwrite=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") ===
        "overwrite" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("overwrite=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "overwrite"
    ) {
      if (
        /^--?(overwrite)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(overwrite)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.overwrite =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.overwrite = true;
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("all=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "all" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("all=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "all"
    ) {
      if (
        /^--?(all)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(all)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.all =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.all = true;
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("path=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "path" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("path=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "path"
    ) {
      if (
        /^--?(path)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.path = arg
          .replace(/^--?(path)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.path = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("dryrun=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "dryrun" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("dryrun=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "dryrun"
    ) {
      if (
        /^--?(dryrun)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(dryrun)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.dryRun =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.dryRun = true;
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("diff=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "diff" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("diff=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "diff"
    ) {
      if (
        /^--?(diff)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(diff)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.diff =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.diff = true;
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("view=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "view" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("view=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "view"
    ) {
      if (
        /^--?(view)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(view)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.view =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.view = true;
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("silent=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "silent" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("silent=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "silent"
    ) {
      if (
        /^--?(silent)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(silent)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.silent =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.silent = true;
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("registry=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") ===
        "registry" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("registry=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "registry"
    ) {
      if (
        /^--?(registry)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.registry = arg
          .replace(/^--?(registry)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.registry = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    }
  }

  let optionsIndex: number =
    Math.max(
      0,
      args
        .slice(2)
        .findIndex(
          arg =>
            arg.startsWith("-") &&
            /^(cwd|overwrite|all|path|dryrun|diff|view|silent|registry)=?.*$/.test(
              arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
            )
        )
    ) + 2;

  let argsIndex: number =
    optionsIndex > 2
      ? 2
      : Math.max(
          0,
          args
            .slice(optionsIndex)
            .findLastIndex(
              arg =>
                arg.startsWith("-") &&
                /^(cwd|overwrite|all|path|dryrun|diff|view|silent|registry)=?.*$/.test(
                  arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
                )
            )
        ) + 2;

  let components: string[] | undefined = [] as string[] | undefined;

  if (argsIndex + 0 < args.length && argsIndex + 0 !== optionsIndex) {
    components = args
      .slice(
        argsIndex + 0,
        (optionsIndex > argsIndex ? optionsIndex : args.length) - 1
      )
      .join(" ")
      .split(",")
      .map(item =>
        item
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
      )
      .filter(Boolean) as string[] | undefined;
  }

  await showBanner();
  await executeUpdate();

  if (!isHelp()) {
    writeLine("");
    if (isDevelopment || isDebug || env.LOG_LEVEL === "debug") {
      writeLine(
        textColors.body.tertiary(
          "Debug mode is enabled. Additional debug information may be logged to the console."
        )
      );
    }
  }

  if (!isInteractive) {
    const failures: string[] = [];
  }

  if (options.help) {
    return showHelp();
  } else {
    return withCommand("add", ["add"], [options, components], handleAdd);
  }
}
