/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import { showBanner } from "shell-shock:banner/init";
import { textColors, writeLine } from "shell-shock:console";
import { env, isDebug, isDevelopment } from "shell-shock:env";
import { showHelp } from "shell-shock:help/init";
import {
  type GlobalOptions,
  isHelp,
  useArgs,
  useGlobalOptions,
  withCommand
} from "shell-shock:state";
import { executeUpdate } from "shell-shock:update";
import { isInteractive } from "shell-shock:utils";
import handleInit from "../../../src/commands/init/command";

// Generated by Power Plant
// Please do not edit this file manually - it will be automatically overwritten
// the next time the generator is run.

export interface InitOptions extends GlobalOptions {
  /**
   * The working directory.
   */
  cwd?: string | undefined;
  /**
   * Component output path, relative to the working directory.
   */
  path?: string | undefined;
  /**
   * Registry API URL.
   */
  registry?: string | undefined;
  /**
   * Overwrite an existing Cyclone configuration.
   */
  force?: boolean | undefined;
  /**
   * Mute non-essential output.
   */
  silent?: boolean | undefined;
}

/**
 * The Initialize (cyclone-ui init) command.
 *
 * @remarks
 * Initialize Cyclone UI in a project.
 *
 * @title Initialize
 *
 * @param args - The command-line arguments passed to the command.
 */
export async function handler(args: string[] = useArgs()) {
  const options = {
    ...useGlobalOptions()
  } as unknown as InitOptions;

  for (let i = 0; i < args.slice(0).length; i++) {
    const arg: string =
      args[i].length > 3 && args[i].startsWith("--")
        ? `--${args[i].slice(2).replaceAll("-", "").toLowerCase().replaceAll("-", "").replaceAll("_", "")}`
        : args[i].length > 2 && args[i].startsWith("-")
          ? `-${args[i].slice(1).replaceAll("-", "")}`
          : args[i];
    if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd"
    ) {
      if (
        /^--?(cwd)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.cwd = arg
          .replace(/^--?(cwd)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.cwd = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("path=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "path" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("path=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "path"
    ) {
      if (
        /^--?(path)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.path = arg
          .replace(/^--?(path)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.path = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("registry=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") ===
        "registry" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("registry=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "registry"
    ) {
      if (
        /^--?(registry)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.registry = arg
          .replace(/^--?(registry)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.registry = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("force=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "force" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("force=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "force"
    ) {
      if (
        /^--?(force)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(force)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.force =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.force = true;
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("silent=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "silent" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("silent=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "silent"
    ) {
      if (
        /^--?(silent)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        const value = arg
          .replace(/^--?(silent)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
          .toLowerCase();
        options.silent =
          !!value &&
          value.toLowerCase() !== "false" &&
          value.toLowerCase() !== "f" &&
          value.toLowerCase() !== "no" &&
          value !== "n" &&
          value.toLowerCase() !== "0" &&
          value.toLowerCase() !== "off" &&
          value !== "disable" &&
          value.toLowerCase() !== "disabled" &&
          value.toLowerCase() !== "never" &&
          (Number.isNaN(Number.parseFloat(value)) ||
            Number.parseFloat(value) > 0);
      } else {
        options.silent = true;
      }
    }
  }

  let optionsIndex: number =
    Math.max(
      0,
      args
        .slice(2)
        .findIndex(
          arg =>
            arg.startsWith("-") &&
            /^(cwd|path|registry|force|silent)=?.*$/.test(
              arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
            )
        )
    ) + 2;

  let argsIndex: number =
    optionsIndex > 2
      ? 2
      : Math.max(
          0,
          args
            .slice(optionsIndex)
            .findLastIndex(
              arg =>
                arg.startsWith("-") &&
                /^(cwd|path|registry|force|silent)=?.*$/.test(
                  arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
                )
            )
        ) + 2;

  let components: string[] | undefined = [] as string[] | undefined;

  if (argsIndex + 0 < args.length && argsIndex + 0 !== optionsIndex) {
    components = args
      .slice(
        argsIndex + 0,
        (optionsIndex > argsIndex ? optionsIndex : args.length) - 1
      )
      .join(" ")
      .split(",")
      .map(item =>
        item
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
      )
      .filter(Boolean) as string[] | undefined;
  }

  await showBanner();
  await executeUpdate();

  if (!isHelp()) {
    writeLine("");
    if (isDevelopment || isDebug || env.LOG_LEVEL === "debug") {
      writeLine(
        textColors.body.tertiary(
          "Debug mode is enabled. Additional debug information may be logged to the console."
        )
      );
    }
  }

  if (!isInteractive) {
    const failures: string[] = [];
  }

  if (options.help) {
    return showHelp();
  } else {
    return withCommand("init", ["init"], [options, components], handleInit);
  }
}
