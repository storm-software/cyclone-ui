/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import { showBanner } from "shell-shock:banner/view";
import { error, italic, textColors, writeLine } from "shell-shock:console";
import { env, isDebug, isDevelopment } from "shell-shock:env";
import { showHelp } from "shell-shock:help/view";
import { isCancel, text } from "shell-shock:prompts";
import {
  type GlobalOptions,
  isHelp,
  useArgs,
  useGlobalOptions,
  withCommand
} from "shell-shock:state";
import { executeUpdate } from "shell-shock:update";
import { isInteractive } from "shell-shock:utils";
import handleView from "../../../src/commands/view/command";

// Generated by Power Plant
// Please do not edit this file manually - it will be automatically overwritten
// the next time the generator is run.

export interface ViewOptions extends GlobalOptions {
  /**
   * The working directory.
   */
  cwd?: string | undefined;
  /**
   * Registry API URL.
   */
  registry?: string | undefined;
}

/**
 * The View Registry Components (cyclone-ui view) command.
 *
 * @remarks
 * View component metadata and files from the registry.
 *
 * @title View Registry Components
 *
 * @param args - The command-line arguments passed to the command.
 */
export async function handler(args: string[] = useArgs()) {
  const options = {
    ...useGlobalOptions()
  } as unknown as ViewOptions;

  for (let i = 0; i < args.slice(0).length; i++) {
    const arg: string =
      args[i].length > 3 && args[i].startsWith("--")
        ? `--${args[i].slice(2).replaceAll("-", "").toLowerCase().replaceAll("-", "").replaceAll("_", "")}`
        : args[i].length > 2 && args[i].startsWith("-")
          ? `-${args[i].slice(1).replaceAll("-", "")}`
          : args[i];
    if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("cwd=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "cwd"
    ) {
      if (
        /^--?(cwd)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.cwd = arg
          .replace(/^--?(cwd)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.cwd = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    } else if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("registry=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") ===
        "registry" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("registry=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "registry"
    ) {
      if (
        /^--?(registry)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.registry = arg
          .replace(/^--?(registry)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      } else if (args.length > i + 1) {
        options.registry = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "");
      }
    }
  }

  let optionsIndex: number =
    Math.max(
      0,
      args
        .slice(2)
        .findIndex(
          arg =>
            arg.startsWith("-") &&
            /^(cwd|registry)=?.*$/.test(
              arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
            )
        )
    ) + 2;

  let argsIndex: number =
    optionsIndex > 2
      ? 2
      : Math.max(
          0,
          args
            .slice(optionsIndex)
            .findLastIndex(
              arg =>
                arg.startsWith("-") &&
                /^(cwd|registry)=?.*$/.test(
                  arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
                )
            )
        ) + 2;

  let components: string[] = [] as string[];

  if (argsIndex + 0 < args.length && argsIndex + 0 !== optionsIndex) {
    components = args
      .slice(
        argsIndex + 0,
        (optionsIndex > argsIndex ? optionsIndex : args.length) - 1
      )
      .join(" ")
      .split(",")
      .map(item =>
        item
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "")
      )
      .filter(Boolean) as string[];
  }

  await showBanner();
  await executeUpdate();

  if (!isHelp()) {
    writeLine("");
    if (isDevelopment || isDebug || env.LOG_LEVEL === "debug") {
      writeLine(
        textColors.body.tertiary(
          "Debug mode is enabled. Additional debug information may be logged to the console."
        )
      );
    }
  }

  if (!isInteractive) {
    const failures: string[] = [];

    if (!components) {
      failures.push('Missing required "components" positional argument');
    } else if (components.length === 0) {
      failures.push(
        'No values were provided to the required "components" array positional argument'
      );
    }
  } else if (!isHelp() && (!components || components.length === 0)) {
    writeLine("");

    if (!components) {
      const value = await text({
        message: `Please provide a value for the ${italic("components")} argument`,
        description: `Component names to view.`,
        validate(val) {
          if (!val || val.trim() === "") {
            return "A value must be provided for this argument";
          }

          return null;
        }
      });
      if (isCancel(value)) {
        return;
      }

      components = value;
    } else if (components.length === 0) {
      const value = await text({
        message: `Please provide one or more values for the ${italic("components")} argument (values are separated by a \",\" character)`,
        description: `Component names to view.`,
        validate(val) {
          if (!val || val.trim() === "") {
            return "A value must be provided for this argument";
          }
          if (
            val
              .split(",")
              .map(v => v.trim())
              .filter(Boolean).length === 0
          ) {
            return "At least one value must be provided for this argument";
          }

          return undefined;
        }
      });
      if (isCancel(value)) {
        return;
      }

      components = value
        .split(",")
        .map(value => value.trim())
        .filter(Boolean);
    }
    writeLine("");

    const failures: string[] = [];

    if (!components) {
      failures.push('Missing required "components" positional argument');
    } else if (components.length === 0) {
      failures.push(
        'No values were provided to the required "components" array positional argument'
      );
    }
    if (failures.length > 0) {
      error(
        `The following validation failures were found while processing the user provided input, and must be corrected before the ${italic("View Registry Components")} command can be executed: \n\n${failures.map(failure => " - " + failure).join("\n")}`
      );
      options.help = true;
    }
  }

  if (options.help) {
    return showHelp();
  } else {
    return withCommand("view", ["view"], [options, components], handleView);
  }
}
