/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import { showBanner } from "shell-shock:banner/gc";
import { error, italic, textColors, writeLine } from "shell-shock:console";
import { env, isDebug, isDevelopment } from "shell-shock:env";
import { showHelp } from "shell-shock:help/gc";
import { isCancel, select } from "shell-shock:prompts";
import {
  type GlobalOptions,
  isHelp,
  useArgs,
  useGlobalOptions,
  withCommand
} from "shell-shock:state";
import { executeUpdate } from "shell-shock:update";
import { isInteractive } from "shell-shock:utils";
import handleGC from "../../../src/commands/gc/command";

// Generated by Shell Shock
// Do not edit this file manually - it will be overwritten automatically.
// Automatic overwrites can be disabled by using the `output.overwrite` option.

export interface GCOptions extends GlobalOptions {
  /**
   * The type of environment paths to clean up. Can be "data", "cache", "logs", "temp", or "all" to clean all types.
   */
  type?: "data" | "cache" | "logs" | "temp" | "all" | undefined;
}

/**
 * The Garbage Collection (powerlines gc) command.
 *
 * @remarks
 * Perform garbage collection to clean up unnecessary system files based on the specified options. This can help free up disk space and improve system performance. Please note: the actual types of environment paths available for cleanup may vary based on the project's configuration and the underlying system's operating system.
 *
 * @title Garbage Collection
 *
 * @param args - The command-line arguments passed to the command.
 */
export async function handler(args: string[] = useArgs()) {
  const options = {
    ...useGlobalOptions()
  } as unknown as GCOptions;

  for (let i = 0; i < args.slice(0).length; i++) {
    const arg: string =
      args[i].length > 3 && args[i].startsWith("--")
        ? `--${args[i].slice(2).replaceAll("-", "").toLowerCase().replaceAll("-", "").replaceAll("_", "")}`
        : args[i].length > 2 && args[i].startsWith("-")
          ? `-${args[i].slice(1).replaceAll("-", "")}`
          : args[i];
    if (
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("type=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "type" ||
      arg
        .toLowerCase()
        .replaceAll("-", "")
        .replaceAll("_", "")
        .startsWith("type=") ||
      arg.toLowerCase().replaceAll("-", "").replaceAll("_", "") === "type"
    ) {
      if (
        /^--?(type)=/.test(
          "-" + arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
        )
      ) {
        options.type = arg
          .replace(/^--?(type)=/, "")
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "") as "data" | "cache" | "logs" | "temp" | "all";
      } else if (args.length > i + 1) {
        options.type = args[++i]
          .trim()
          .replace(/^("|')/, "")
          .replace(/("|')$/, "") as "data" | "cache" | "logs" | "temp" | "all";
      }
    }
  }

  let optionsIndex: number =
    Math.max(
      0,
      args
        .slice(2)
        .findIndex(
          arg =>
            arg.startsWith("-") &&
            /^(type)=?.*$/.test(
              arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
            )
        )
    ) + 2;

  let argsIndex: number =
    optionsIndex > 2
      ? 2
      : Math.max(
          0,
          args
            .slice(optionsIndex)
            .findLastIndex(
              arg =>
                arg.startsWith("-") &&
                /^(type)=?.*$/.test(
                  arg.toLowerCase().replaceAll("-", "").replaceAll("_", "")
                )
            )
        ) + 2;

  let type: "data" | "cache" | "logs" | "temp" | "all" = "all" as
    | "data"
    | "cache"
    | "logs"
    | "temp"
    | "all";

  if (argsIndex + 0 < args.length && argsIndex + 0 !== optionsIndex) {
    type = args[argsIndex + 0] as "data" | "cache" | "logs" | "temp" | "all";
  }

  await showBanner();
  await executeUpdate();

  if (!isHelp()) {
    writeLine("");
    if (isDevelopment || isDebug || env.LOG_LEVEL === "debug") {
      writeLine(
        textColors.body.tertiary(
          "Debug mode is enabled. Additional debug information may be logged to the console."
        )
      );
    }
  }

  if (!isInteractive) {
    const failures: string[] = [];
    if (!["data", "cache", "logs", "temp", "all"].includes(options.type)) {
      failures.push(
        `Invalid value provided for the \"type\" option - valid values include: data, cache, logs, temp, and all; provided: ${options.type}`
      );
    }

    if (!type) {
      failures.push('Missing required "type" positional argument');
    }
  } else if (!isHelp() && type === undefined) {
    writeLine("");

    if (!type) {
      const value = await select({
        message: `Please select a value for the ${italic("type")} argument`,
        description: `An argument that allows the user to specify a custom Type string value.`,

        options: [
          {
            value: "data",
            label: "data",
            description: `An argument that allows the user to specify a custom Type s...`
          },
          {
            value: "cache",
            label: "cache",
            description: `An argument that allows the user to specify a custom Type s...`
          },
          {
            value: "logs",
            label: "logs",
            description: `An argument that allows the user to specify a custom Type s...`
          },
          {
            value: "temp",
            label: "temp",
            description: `An argument that allows the user to specify a custom Type s...`
          },
          {
            value: "all",
            label: "all",
            description: `An argument that allows the user to specify a custom Type s...`
          }
        ]
      });
      if (isCancel(value)) {
        return;
      }

      type = value;
    }
    writeLine("");

    const failures: string[] = [];
    if (!["data", "cache", "logs", "temp", "all"].includes(options.type)) {
      failures.push(
        `Invalid value provided for the \"type\" option - valid values include: data, cache, logs, temp, and all; provided: ${options.type}`
      );
    }

    if (!type) {
      failures.push('Missing required "type" positional argument');
    }
    if (failures.length > 0) {
      error(
        `The following validation failures were found while processing the user provided input, and must be corrected before the ${italic("Garbage Collection")} command can be executed: \n\n${failures.map(failure => " - " + failure).join("\n")}`
      );
      options.help = true;
    }
  }

  if (options.help) {
    return showHelp();
  } else {
    return withCommand("gc", ["gc"], [options, type], handleGC);
  }
}
