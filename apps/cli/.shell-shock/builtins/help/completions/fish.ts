/**
 * A collection of utility functions that assist in displaying help information for the Completions - Fish command.
 *
 * @module shell-shock:help/completions/fish
 */

/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import {
  bold,
  help,
  inlineCode,
  inverse,
  link,
  splitText,
  table,
  textColors,
  underline,
  writeLine
} from "shell-shock:console";
import { isUnicodeSupported } from "shell-shock:utils";

// Generated by Power Plant
// Please do not edit this file manually - it will be automatically overwritten
// the next time the generator is run.

/**
 * Utility functions for displaying help information for the Completions - Fish command.
 *
 * @remarks
 * This module contains utility functions that assist in displaying help information for the Completions - Fish command. The main function exported by this module is the `showHelp` function, which can be used to display help information for the specified command or application. This function can be called from within the command's handler or from any other part of the application where help information needs to be displayed.
 */
/**
 * Display help information for the Completions - Fish command.
 */
export function showHelp() {
  writeLine(
    `${bold(textColors.heading.secondary("Tags: "))} ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`,
    { padding: 0 }
  );
  writeLine(bold(textColors.heading.secondary("Common Options:")));
  table([]);
  writeLine("");

  writeLine(
    textColors.body.tertiary(
      "The following commands are available through the Cyclone User Interface command-line interface:"
    )
  );
  writeLine("");

  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions - Fish Configuration")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Update the current system\'s Fish shell configuration to include completions for the Cyclone User Interface command-line interface.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("completions")} ${textColors.usage.command("fish")} ${textColors.usage.command("config")} ${textColors.usage.args("<path>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("completions")} ${textColors.usage.command("fish")} ${textColors.usage.command("config")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<path>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--display"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Should the generated completion script be written to console output instead of an actual file on disk? (default: false).`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions - Fish Script")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Generate a Fish completion script for the Cyclone User Interface command-line interface.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("completions")} ${textColors.usage.command("fish")} ${textColors.usage.command("script")} ${textColors.usage.args("<path>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("completions")} ${textColors.usage.command("fish")} ${textColors.usage.command("script")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<path>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--display"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Should the generated completion script be written to console output instead of an actual file on disk? (default: false).`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.body.tertiary(
      `More information can be found in the Completions - Fish command documentation at ${link("https://docs.stormsoftware.com/projects/cyclone-ui/reference/cli/completions/fish")}${textColors.body.tertiary(".")}`
    ),
    { padding: 0 }
  );
  writeLine("");
  help(
    `Running a specific command with the help flag (via: ${inlineCode("cyclone-ui completions fish <command> --help")}) or the help command with the specific command as arguments (via: ${inlineCode("cyclone-ui completions fish help <command>")}) will provide additional information that is specific to that command.`
  );
  writeLine("");
}
