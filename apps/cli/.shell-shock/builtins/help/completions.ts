/**
 * A collection of utility functions that assist in displaying help information for the Completions command.
 *
 * @module shell-shock:help/completions
 */

/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import {
  bold,
  help,
  inlineCode,
  inverse,
  link,
  splitText,
  table,
  textColors,
  underline,
  writeLine
} from "shell-shock:console";
import { isUnicodeSupported } from "shell-shock:utils";

// Generated by Shell Shock
// Do not edit this file manually - it will be overwritten automatically.
// Automatic overwrites can be disabled by using the `output.overwrite` option.

/**
 * Utility functions for displaying help information for the Completions command.
 *
 * @remarks
 * This module contains utility functions that assist in displaying help information for the Completions command. The main function exported by this module is the `showHelp` function, which can be used to display help information for the specified command or application. This function can be called from within the command's handler or from any other part of the application where help information needs to be displayed.
 */
/**
 * Display help information for the Completions command.
 */
export function showHelp() {
  writeLine(
    `${bold(textColors.heading.secondary("Tags: "))} ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`,
    { padding: 0 }
  );
  writeLine(bold(textColors.heading.secondary("Common Options:")));
  table([]);
  writeLine("");

  writeLine(
    textColors.body.tertiary(
      "The following commands are available through the Powerlines command-line interface:"
    )
  );
  writeLine("");

  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions - Bash")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Commands to setup bash completions for the Powerlines command-line interface.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("completions")} ${textColors.usage.command("bash")} ${textColors.usage.dynamic("<command>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Commands:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("config"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Update the current system\'s bash shell configuration to include completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("script"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Generate a bash completion script for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions - Fish")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Commands to setup Fish completions for the Powerlines command-line interface.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("completions")} ${textColors.usage.command("fish")} ${textColors.usage.dynamic("<command>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Commands:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("config"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Update the current system\'s Fish shell configuration to include completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("script"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Generate a Fish completion script for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions - PowerShell")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Commands to setup PowerShell completions for the Powerlines command-line interface.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("completions")} ${textColors.usage.command("powershell")} ${textColors.usage.dynamic("<command>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Commands:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("config"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Update the current system\'s PowerShell configuration to include completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("script"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Generate a PowerShell completion script for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions - Zsh")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Commands to setup Zsh completions for the Powerlines command-line interface.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("completions")} ${textColors.usage.command("zsh")} ${textColors.usage.dynamic("<command>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Commands:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("config"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Update the current system\'s Zsh shell configuration to include completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("script"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Generate a Zsh completion script for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.body.tertiary(
      `More information can be found in the Completions command documentation at ${link("https://docs.stormsoftware.com/projects/powerlines/reference/cli/completions")}${textColors.body.tertiary(".")}`
    ),
    { padding: 0 }
  );
  writeLine("");
  help(
    `Running a specific command with the help flag (via: ${inlineCode("powerlines completions <command> --help")}) or the help command with the specific command as arguments (via: ${inlineCode("powerlines completions help <command>")}) will provide additional information that is specific to that command.`
  );
  writeLine("");
}
