/**
 * A collection of utility functions that assist in displaying help information for the Registry Commands command.
 *
 * @module shell-shock:help/registry
 */

/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import {
  bold,
  help,
  inlineCode,
  link,
  splitText,
  table,
  textColors,
  underline,
  writeLine
} from "shell-shock:console";
import { isUnicodeSupported } from "shell-shock:utils";

// Generated by Power Plant
// Please do not edit this file manually - it will be automatically overwritten
// the next time the generator is run.

/**
 * Utility functions for displaying help information for the Registry Commands command.
 *
 * @remarks
 * This module contains utility functions that assist in displaying help information for the Registry Commands command. The main function exported by this module is the `showHelp` function, which can be used to display help information for the specified command or application. This function can be called from within the command's handler or from any other part of the application where help information needs to be displayed.
 */
/**
 * Display help information for the Registry Commands command.
 */
export function showHelp() {
  writeLine(bold(textColors.heading.secondary("Common Options:")));
  table([]);
  writeLine("");

  writeLine(
    textColors.body.tertiary(
      "The following commands are available through the Cyclone User Interface command-line interface:"
    )
  );
  writeLine("");

  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " ✓  " : "") + `${underline("Validate Registry")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Validate a local registry manifest and its source files.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("registry")} ${textColors.usage.command("validate")} ${textColors.usage.args("<registry>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("registry")} ${textColors.usage.command("validate")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<registry>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.body.tertiary(
      `More information can be found in the Registry Commands command documentation at ${link("https://docs.stormsoftware.com/projects/cyclone-ui/reference/cli/registry")}${textColors.body.tertiary(".")}`
    ),
    { padding: 0 }
  );
  writeLine("");
  help(
    `Running a specific command with the help flag (via: ${inlineCode("cyclone-ui registry <command> --help")}) or the help command with the specific command as arguments (via: ${inlineCode("cyclone-ui registry help <command>")}) will provide additional information that is specific to that command.`
  );
  writeLine("");
}
