/**
 * A collection of utility functions that assist in displaying help information for the Cyclone User Interface command-line interface application.
 *
 * @module shell-shock:help
 */

/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import {
  bold,
  help,
  inlineCode,
  inverse,
  link,
  splitText,
  table,
  textColors,
  underline,
  writeLine
} from "shell-shock:console";
import { isUnicodeSupported } from "shell-shock:utils";

// Generated by Power Plant
// Please do not edit this file manually - it will be automatically overwritten
// the next time the generator is run.

/**
 * Utility functions for displaying help information for the Cyclone User Interface application.
 *
 * @remarks
 * This module contains utility functions that assist in displaying help information for the Cyclone User Interface application. The main function exported by this module is the `showHelp` function, which can be used to display help information for the specified command or application. This function can be called from within the command's handler or from any other part of the application where help information needs to be displayed.
 */
/**
 * Display help information for the Cyclone User Interface application.
 */
export function showHelp() {
  writeLine(bold(textColors.heading.secondary("Common Options:")));
  table([
    [
      {
        value: textColors.body.primary("--color, --colors"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Force colored terminal output (env: CYCLONE_UI_COLOR, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-color, --no-colors"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Force disable colored terminal output (env: CYCLONE_UI_NO_COLOR, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-?, -h, --help"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Show help information (default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-i, --interact, --interactive"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Enable interactive mode - will be set to false if running in a CI pipeline (env: CYCLONE_UI_INTERACTIVE, default: true).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-interactive, --non-interactive"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Disable interactive mode - will be set to true if running in a CI pipeline (env: CYCLONE_UI_NON_INTERACTIVE, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--hide-banner, --no-banner"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Do not display the application banner displayed while running the CLI - will be set to true if running in a CI pipeline (env: CYCLONE_UI_NO_BANNER, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-color, --no-colors"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Force disable colored terminal output (env: CYCLONE_UI_NO_COLOR, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-interactive, --non-interactive"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Disable interactive mode - will be set to true if running in a CI pipeline (env: CYCLONE_UI_NON_INTERACTIVE, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-V, --verbose"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Enable verbose output (env: CYCLONE_UI_VERBOSE, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-v, --version"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Show the version of the application (default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-y, --yes"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Skip all prompts by answering yes to all of them - will be set to true if running in a CI pipeline (env: CYCLONE_UI_YES, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);
  writeLine("");

  writeLine(
    textColors.body.tertiary(
      "The following commands are available through the Cyclone User Interface command-line interface:"
    )
  );
  writeLine("");

  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " ➕  " : "") + `${underline("Add")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(splitText(`Adds a new component to the project.`))
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("add")} ${textColors.usage.args("<components...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("add")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<components...>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--all"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Add every available component.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--diff"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Show the changes as a diff without writing files.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--dry-run"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Preview changes without writing files.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--overwrite"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Overwrite existing files.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--path <path>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Override the configured component output path.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--registry <registry>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Override the configured registry API URL.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--silent"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Mute non-essential output.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--view"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Show resolved file contents without writing files.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🏗  " : "") + `${underline("Build Registry")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Build registry item JSON files for distribution.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("build")} ${textColors.usage.args("<registry>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("build")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<registry>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--output <output>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Output directory.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " ±  " : "") + `${underline("Diff Components")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Compare installed components with the registry.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("diff")} ${textColors.usage.args("<components...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("diff")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<components...>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--registry <registry>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Registry API URL.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🕮  " : "") +
        `${underline("Component Documentation")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Get documentation links for registry components.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("docs")} ${textColors.usage.args("<components...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("docs")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<components...>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--json"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Output machine-readable JSON.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--registry <registry>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Registry API URL.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " ⓘ  " : "") + `${underline("Project Information")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Show Cyclone UI project configuration and installed components.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("info")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--json"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Output machine-readable JSON.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🌀  " : "") + `${underline("Initialize")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(splitText(`Initialize Cyclone UI in a project.`))
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("init")} ${textColors.usage.args("<components...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("init")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<components...>")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("create")} ${textColors.usage.args("<components...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("create")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<components...>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--force"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Overwrite an existing Cyclone configuration.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--path <path>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Component output path, relative to the working directory.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--registry <registry>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Registry API URL.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--silent"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Mute non-essential output.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(textColors.heading.primary(`${underline("Registry Commands")}`));
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `A collection of available Registry commands that are included in the Cyclone User Interface command-line application.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("registry")} ${textColors.usage.dynamic("<command>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Commands:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("validate"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Validate a local registry manifest and its source files.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " ⌕  " : "") + `${underline("Search Registry")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Search components in the Cyclone registry.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("search")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("list")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--json"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Output machine-readable JSON.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--limit <limit>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Maximum number of results.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--offset <offset>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Number of results to skip.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--query <query>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Text to match against names, descriptions, and tags.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--registry <registry>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Registry API URL.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " ◉  " : "") +
        `${underline("View Registry Components")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`View component metadata and files from the registry.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("view")} ${textColors.usage.args("<components...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("view")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<components...>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--cwd <cwd>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`The working directory.`),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--registry <registry>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(`Registry API URL.`),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Commands for generating shell completion scripts for Cyclone User Interface.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("completions")} ${textColors.usage.dynamic("<command>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Commands:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("bash"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Commands to setup bash completions for the Cyclone User Interface command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("fish"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Commands to setup Fish completions for the Cyclone User Interface command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("powershell"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Commands to setup PowerShell completions for the Cyclone User Interface command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      { value: textColors.body.primary("zsh"), align: "right", border: "none" },
      {
        value: textColors.body.tertiary(
          `Commands to setup Zsh completions for the Cyclone User Interface command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🛈  " : "") +
        `${underline("Help")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Display command usage details and other useful information to the user.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("help")} ${textColors.usage.args("<commands...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("help")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<commands...>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖒  " : "") +
        `${underline("Update")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `A command for checking and updating the version of the Cyclone User Interface command-line interface application.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("update")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ cyclone-ui")} ${textColors.usage.command("upgrade")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--latest"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Whether to update to the latest version (default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--version <version>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `A specific application version to update to (default: \"latest\").`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.body.tertiary(
      `More information can be found in the Cyclone User Interface documentation at ${link("https://docs.stormsoftware.com/projects/cyclone-ui/reference/cli")}${textColors.body.tertiary(".")}`
    ),
    { padding: 0 }
  );
  writeLine("");
  help(
    `Running a specific command with the help flag (via: ${inlineCode("cyclone-ui <command> --help")}) or the help command with the specific command as arguments (via: ${inlineCode("cyclone-ui help <command>")}) will provide additional information that is specific to that command.`
  );
  writeLine("");
}
