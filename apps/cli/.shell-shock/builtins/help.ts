/**
 * A collection of utility functions that assist in displaying help information for the Powerlines command-line interface application.
 *
 * @module shell-shock:help
 */

/* eslint-disable */
/* oxlint-disable */
// biome-ignore lint: disable

import {
  bold,
  help,
  inlineCode,
  inverse,
  link,
  splitText,
  table,
  textColors,
  underline,
  writeLine
} from "shell-shock:console";
import { isUnicodeSupported } from "shell-shock:utils";

// Generated by Shell Shock
// Do not edit this file manually - it will be overwritten automatically.
// Automatic overwrites can be disabled by using the `output.overwrite` option.

/**
 * Utility functions for displaying help information for the Powerlines application.
 *
 * @remarks
 * This module contains utility functions that assist in displaying help information for the Powerlines application. The main function exported by this module is the `showHelp` function, which can be used to display help information for the specified command or application. This function can be called from within the command's handler or from any other part of the application where help information needs to be displayed.
 */
/**
 * Display help information for the Powerlines application.
 */
export function showHelp() {
  writeLine(bold(textColors.heading.secondary("Common Options:")));
  table([
    [
      {
        value: textColors.body.primary("--color, --colors"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Force colored terminal output (env: POWERLINES_COLOR, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-color, --no-colors"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Force disable colored terminal output (env: POWERLINES_NO_COLOR, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-?, -h, --help"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Show help information (default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-i, --interact, --interactive"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Enable interactive mode - will be set to false if running in a CI pipeline (env: POWERLINES_INTERACTIVE, default: true).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-interactive, --non-interactive"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Disable interactive mode - will be set to true if running in a CI pipeline (env: POWERLINES_NON_INTERACTIVE, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--hide-banner, --no-banner"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Do not display the application banner displayed while running the CLI - will be set to true if running in a CI pipeline (env: POWERLINES_NO_BANNER, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-color, --no-colors"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Force disable colored terminal output (env: POWERLINES_NO_COLOR, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--no-interactive, --non-interactive"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Disable interactive mode - will be set to true if running in a CI pipeline (env: POWERLINES_NON_INTERACTIVE, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-V, --verbose"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Enable verbose output (env: POWERLINES_VERBOSE, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-v, --version"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Show the version of the application (default: false).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("-y, --yes"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Skip all prompts by answering yes to all of them - will be set to true if running in a CI pipeline (env: POWERLINES_YES, default: false).`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);
  writeLine("");

  writeLine(
    textColors.body.tertiary(
      "The following commands are available through the Powerlines command-line interface:"
    )
  );
  writeLine("");

  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🏗  " : "") + `${underline("Build")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Builds the project for production deployment.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("build")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to build.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🗑  " : "") + `${underline("Clean")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Cleans the project by removing build artifacts and temporary files.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("clean")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to clean.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " ⬚  " : "") + `${underline("Create Project")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(splitText(`Create a new Powerlines project.`))
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("create")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to create.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🛪  " : "") + `${underline("Deploy")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Deploys the project and its generated assets/artifacts.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("deploy")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("deployment")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to deploy.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🕮  " : "") +
        `${underline("Generate Documentation")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Generate documentation for the project\'s source code.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("docs")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to generate documentation for.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🗶  " : "") + `${underline("Garbage Collection")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Perform garbage collection to clean up unnecessary system files based on the specified options. This can help free up disk space and improve system performance. Please note: the actual types of environment paths available for cleanup may vary based on the project\'s configuration and the underlying system\'s operating system.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("gc")} ${textColors.usage.args("<data|cache|logs|temp|all>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("gc")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<data|cache|logs|temp|all>")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("garbage-collection")} ${textColors.usage.args("<data|cache|logs|temp|all>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("garbage-collection")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<data|cache|logs|temp|all>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--type <data|cache|logs|temp|all>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The type of environment paths to clean up.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🗹  " : "") + `${underline("Lint Source Code")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Lint the project\'s source code for style and syntax errors.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("lint")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to lint.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖉  " : "") + `${underline("Prepare Artifacts")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Generate the project\'s required build and deployment artifacts.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("prepare")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to prepare.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖹  " : "") +
        `${underline("Generate Typescript Declarations")}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(`Generate a Typescript declaration (dts) file for the project.`)
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("types")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--root <root>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `The root directory of the project to generate types for.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🗃  " : "") +
        `${underline("Changelog")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(splitText(`Display the Powerlines changelog.`))
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("changelog")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖵  " : "") +
        `${underline("Completions")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Commands for generating shell completion scripts for Powerlines.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("completions")} ${textColors.usage.dynamic("<command>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Commands:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("bash"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Commands to setup bash completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("fish"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Commands to setup Fish completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("powershell"),
        align: "right",
        border: "none"
      },
      {
        value: textColors.body.tertiary(
          `Commands to setup PowerShell completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      { value: textColors.body.primary("zsh"), align: "right", border: "none" },
      {
        value: textColors.body.tertiary(
          `Commands to setup Zsh completions for the Powerlines command-line interface.`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🛈  " : "") +
        `${underline("Help")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `Display command usage details and other useful information to the user.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("help")} ${textColors.usage.args("<commands...>")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("help")} ${textColors.usage.options("[options]")} ${textColors.usage.args("<commands...>")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(
    textColors.heading.primary(
      (isUnicodeSupported ? " 🖒  " : "") +
        `${underline("Update")} - ${textColors.tags.utility ? textColors.tags.utility(inverse(" Utility ")) : textColors.tags.$default(inverse(" Utility "))}`
    )
  );
  writeLine("");
  writeLine(
    textColors.body.tertiary(
      splitText(
        `A command for checking and updating the version of the Powerlines command-line interface application.`
      )
    )
  );
  writeLine("");
  writeLine(bold(textColors.heading.secondary("Usage:")), { padding: 0 });
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("update")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );
  writeLine(
    textColors.body.secondary(
      `${textColors.usage.bin(">_ powerlines")} ${textColors.usage.command("upgrade")} ${textColors.usage.options("[options]")}`
    ),
    { padding: 0 }
  );

  writeLine("");
  writeLine(bold(textColors.heading.secondary("Options:")), { padding: 0 });
  table([
    [
      {
        value: textColors.body.primary("--latest"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `Whether to update to the latest version (default: true).`
        ),
        align: "left",
        border: "none"
      }
    ],
    [
      {
        value: textColors.body.primary("--version <version>"),
        align: "right",
        border: "none",
        maxWidth: "1/3"
      },
      {
        value: textColors.body.tertiary(
          `A specific application version to update to (default: \"latest\").`
        ),
        align: "left",
        border: "none"
      }
    ]
  ]);

  writeLine("");
  writeLine(
    textColors.body.tertiary(
      `More information can be found in the Powerlines documentation at ${link("https://docs.stormsoftware.com/projects/powerlines/reference/cli")}${textColors.body.tertiary(".")}`
    ),
    { padding: 0 }
  );
  writeLine("");
  help(
    `Running a specific command with the help flag (via: ${inlineCode("powerlines <command> --help")}) or the help command with the specific command as arguments (via: ${inlineCode("powerlines help <command>")}) will provide additional information that is specific to that command.`
  );
  writeLine("");
}
